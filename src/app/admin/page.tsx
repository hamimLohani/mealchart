import { PageCard } from "@/components/layout/page-card";

export default function AdminPage() {
  return (
    <PageCard
      eyebrow="Admin Area"
      title="App route map"
      description="Use the admin navigation bar above to move between members, costs, add money, meal editing, chart creation, and notices."
    />
  );
}
